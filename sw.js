const CACHE='product-issue-tracker-v10';
const SHELL=['./','./index.html','./manifest.webmanifest','./icon.svg','./enhancements.js'];
self.addEventListener('install',event=>{self.skipWaiting();event.waitUntil(caches.open(CACHE).then(c=>c.addAll(SHELL)))});
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const u=new URL(event.request.url);if(u.origin!==location.origin)return;if(event.request.mode==='navigate'){event.respondWith(fetch(event.request).then(async r=>{if(!r.ok)return r;const t=await r.text();if(t.includes('./enhancements.js'))return new Response(t,{status:r.status,statusText:r.statusText,headers:{'Content-Type':'text/html; charset=utf-8'}});const x=t.replace('</body>','<script type="module" src="./enhancements.js?v=10"></script></body>');caches.open(CACHE).then(c=>c.put(event.request,new Response(x,{headers:{'Content-Type':'text/html; charset=utf-8'}})));return new Response(x,{status:r.status,statusText:r.statusText,headers:{'Content-Type':'text/html; charset=utf-8'}})}).catch(()=>caches.match('./index.html')));return}event.respondWith(fetch(event.request).then(r=>{if(r.ok)caches.open(CACHE).then(c=>c.put(event.request,r.clone()));return r}).catch(()=>caches.match(event.request)))});